import example.*
test "Punto 1" {
	assert.equals(490000000, festejo.recaudacionTotal())
	assert.equals(410000000,festejo.gananciaNeta())
}

test "Punto 2" {
	assert.equals(49, ronaldo.popularidad())
	assert.equals(54, mbappe.popularidad())
	assert.equals(63000,monumental.capacidad())
}

test "Punto 3" {
	inflacion.porcentajeInflacion(10)
	assert.equals(11000,festejo.precioEntrada())
	
	monumental.avanzarObras(5)
	assert.equals(95,monumental.estadoAvanceObras())
	
	messi.opinarDelPresidente()
	assert.equals(88,messi.popularidad())
	
	mbappe.cumplirAnios()
	assert.equals(26,mbappe.edad())
	
	ronaldo.ubicacionJugador(antartida)
	assert.equals(antartida,ronaldo.ubicacionJugador())
}